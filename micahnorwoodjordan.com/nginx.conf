events {}

http {
	sendfile on;
	include /etc/nginx/mime.types;
	access_log /var/log/nginx/access.log;

	server {
		root /usr/share/nginx/html;
		server_name micahnorwoodjordan.com;
		listen 80;

		location / {
			index index.html;
		}
	}
}
